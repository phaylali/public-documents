{
  "name": "public-documents",
  "type": "module",
  "private": true,
  "scripts": {
    "dev": "bun run scripts/generate-manifest.ts && bun run index.bun.ts",
    "build": "bun run scripts/generate-manifest.ts",
    "deploy": "npx wrangler pages deploy . --project-name=public-documents"
  },
  "devDependencies": {
    "@types/bun": "latest",
    "wrangler": "^4.63.0"
  },
  "peerDependencies": {
    "typescript": "^5"
  },
  "dependencies": {
    "hono": "^4.11.7"
  }
}